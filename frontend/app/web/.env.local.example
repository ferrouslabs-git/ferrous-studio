# Copy to frontend/app/web/.env.local (gitignored) for `npm run dev`.
# Values printed by 'terraform output cognito' for the STAGING pool —
# local dev always authenticates against staging, there is no local Cognito.
VITE_COGNITO_DOMAIN=
VITE_COGNITO_APP_CLIENT_ID=
VITE_APP_ENV=local
